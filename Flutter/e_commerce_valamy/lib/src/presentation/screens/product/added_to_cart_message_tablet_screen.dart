import 'package:flutter/material.dart';

class AddedToCartMessageTabletScreen extends StatelessWidget {
  final TextTheme? textTheme;

  const AddedToCartMessageTabletScreen({super.key, this.textTheme});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
