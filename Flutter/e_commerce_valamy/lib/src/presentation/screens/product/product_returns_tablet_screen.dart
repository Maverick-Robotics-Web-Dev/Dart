import 'package:flutter/material.dart';

class ProductReturnsTabletScreen extends StatelessWidget {
  final TextTheme? textTheme;

  const ProductReturnsTabletScreen({super.key, this.textTheme});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
