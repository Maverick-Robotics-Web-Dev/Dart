import 'package:flutter/material.dart';

class ProductDetailTabletScreen extends StatelessWidget {
  final TextTheme? textTheme;

  const ProductDetailTabletScreen({super.key, this.textTheme});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
