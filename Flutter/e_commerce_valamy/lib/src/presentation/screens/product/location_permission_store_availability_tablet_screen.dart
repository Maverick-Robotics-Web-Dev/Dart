import 'package:flutter/material.dart';

class LocationPermissionStoreAvailabilityTabletScreen extends StatelessWidget {
  final TextTheme? textTheme;

  const LocationPermissionStoreAvailabilityTabletScreen({
    super.key,
    this.textTheme,
  });

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
