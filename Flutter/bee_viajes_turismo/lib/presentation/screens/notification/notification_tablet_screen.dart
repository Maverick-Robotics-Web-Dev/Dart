import 'package:flutter/material.dart';

class NotificationTabletScreen extends StatelessWidget {
  final ThemeData appTheme;

  const NotificationTabletScreen({super.key, required this.appTheme});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
